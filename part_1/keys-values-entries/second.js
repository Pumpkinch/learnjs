// https://learn.javascript.ru/keys-values-entries#podschyot-kolichestva-svoystv-obekta
let user = {
    name: 'John',
    age: 30
};

const count = (obj) => {
    return lot = (Object.keys(obj)).length;
}

alert(count(user));